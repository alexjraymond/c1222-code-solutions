/* exported head */
// pseudocode
// look at the first thing in the array and give it back

// pseudo code with codey words
// return the index of 0 of the input

function head(array) {
  return array[0];
}
