/* exported last */
// pseudocode
// look at the last thing in the array and give it back

// pseudo code with codey words
// return the index of input.length - 1 of the input

function last(array) {
  return array[array.length - 1];
}
