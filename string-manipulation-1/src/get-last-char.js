/* exported getLastChar */

// PseudoCode
// i need to get the LAST letter of the text content
// then i spit it out

// PseudoCode with codey words
// find length of argument... string.length - 1
// return the string at the index of string.length - 1;
// win :)

function getLastChar(string) {
  return string[string.length - 1];
}
