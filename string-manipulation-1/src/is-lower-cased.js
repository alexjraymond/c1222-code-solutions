/* exported isLowerCased */
// PseudoCode
// look at the word
// is it all lowercase? yay
// is it not? boo

// Pseudo Codey Code
// beautiful for loop
//     - if the word is equal to the lowercase version of the word, then return true
//     - else, return false

function isLowerCased(word) {
  if (word === word.toLowerCase()) {
    return true;
  } else {
    return false;
  }
}
