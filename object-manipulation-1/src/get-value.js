/* exported getValue */
// PseudoCode
// we look at where in the object the key property is
// we print that out

// PseudoCode with Codey Words
// one liner, bracket notation, return object @ key

function getValue(object, key) {
  return object[key];
}
